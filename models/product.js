class Product {
  constructor(id, userId, name, image, desc, price) {
    this.id = id;
    this.userId = userId;
    this.name = name;
    this.image = image;
    this.desc = desc;
    this.price = price;
  }
}

export default Product;
