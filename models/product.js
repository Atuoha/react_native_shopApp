class Product {
  constructor(id, userId, name, image, desc, price, colors) {
    this.id = id;
    this.userId = userId;
    this.name = name;
    this.image = image;
    this.desc = desc;
    this.price = price;
    this.colors = colors;
  }
}

export default Product;
